export const environment = {
  production: false,
  // Empty string causes relative URLs - works for same-origin deployment
  // Override with absolute URL (e.g., 'http://localhost:8080') if API is on different host
  apiBaseUrl: ''
};
